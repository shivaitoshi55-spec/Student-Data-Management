# Student-Data-Management
A menu-driven Student Management System built using OOP in Python. It manages student records with add, view, search, update, and delete features, stores data in a CSV file, prevents duplicate entries, and calculates subject-wise average marks.
